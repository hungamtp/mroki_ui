import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  container: {
    marginTop: 150,
  },
  formLogin: {
    display: "block",
  },
  button: {
    marginTop: 30,
    marginBottom: 30,
    display: "flex",
    justifyContent: "center",
  },
  registerButton: {
    marginTop: 30,
    display: "flex",
    justifyContent: "center",
  },
}));
