import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  filterContainer: {
    marginTop: 20,
    height: 150,
  },
  pagination: {
    display: "flex",
    justifyContent: "center",
  },
  totalElement: {
    marginTop: 5,
    marginLeft: 100,
  },
}));
